@model IEnumerable<advanced_jobmatchingtool_webapp.ViewModels.Vraag.VraagKandidaatIndexViewModel>

@{
    ViewData["Title"] = "Index";
}

<style>
    #custtable tbody tr {
        cursor: pointer;
        color: darkslategray;
    }
</style>

<h1>Index</h1>

<p>
    <a asp-action="Create">Aanmaken</a>
</p>
<table id="custtable" class="display cell-border compact">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VraagTekst)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Categorie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubCategorie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SoortAntwoord)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AntwoordOptie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    </tbody>
    @*
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VraagText)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SoortAntwoord)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Categorie.NaamCategorie)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Categorie.NaamSubCategorie)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.AntwoordOptie.OptieTekst)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
   *@
</table>
<link rel="stylesheet" href="https://cdn.datatables.net/1.13.11/css/jquery.dataTables.css" />

@section Scripts {
    <script src="https://cdn.datatables.net/1.13.11/js/jquery.dataTables.js"></script>

    <script>
        $(function () {
            $('#custtable').DataTable({
                "ajax": {
                    //Client Side
                    "url": "/VragenKandidaat/GetAllVragenKandidatenData",
                    "type": "GET",
                    "datatype": "json"
                },
                "columns": [
                    { "data": "id" },
                    { "data": "vraagTekst" },
                    { "data": "categorie" },
                    { "data": "subCategorie" },
                    { "data": "soortAntwoordWeergave", title:"Soort Antwoord" },
                    { "data": "antwoordOptie" },
                    { "data": null, "defaultContent": "<a href='' class='del'>Verwijderen</a>" }
                ]

            });

            $('#custtable tbody').one('click', 'tr', function (e) {
                e.preventDefault();
                var id = $("td:first", this).text();
                if ($(e.target).is("a.del")) {
                    $.get('@Url.Action("Delete", "VragenKandidaat")', { id: id }, function (data) {
                        $("body").html(data);
                    });
                    return;
                }
                $.get('@Url.Action("Edit", "VragenKandidaat")', { id: id }, function (data) {
                    $("body").html(data);
                })
            });

        });
    </script>
}

<a asp-controller="HomeBeheer" asp-action="Index" class="btn btn-secondary">Terug</a>
